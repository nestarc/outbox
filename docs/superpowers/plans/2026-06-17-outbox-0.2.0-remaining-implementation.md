# outbox 0.2.0 Remaining Implementation Plan

## Goal

Implement the remaining v0.2.0 specification after broker-capable transport mode:

- stable event metadata/envelope;
- tenant propagation;
- local handler context;
- admin and DLQ operations;
- observability hooks;
- optional PostgreSQL LISTEN/NOTIFY wakeup;
- migration SQL, docs, and verification.

## Constraints

- Preserve v0.1 local handler compatibility where practical.
- Keep broker mode changes already implemented.
- Prefer additive public API changes.
- Use TDD for each behavior before production edits.
- Avoid hard dependency growth unless the feature cannot work without it.

## Work Items

- [x] Add RED unit tests for emitter metadata, tenant precedence, hook calls, and handler context.
- [x] Implement `OutboxEmitOptions`, metadata columns, `OutboxRecord` metadata fields, and SQL migration.
- [x] Add tenant provider option and local handler context propagation.
- [x] Add RED unit tests for `OutboxAdminService`.
- [x] Implement `OutboxAdminService` and export it from module and package entrypoint.
- [x] Add RED unit tests for observability hooks around delivery success/failure/dead-letter.
- [x] Implement observability hook dispatch with hook-error isolation.
- [x] Add optional LISTEN/NOTIFY wakeup support with no mandatory `pg` dependency.
- [x] Update README and CHANGELOG for v0.2.0 behavior.
- [x] Run unit tests, lint, build, and e2e tests.
