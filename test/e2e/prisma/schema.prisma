datasource db {
  provider = "postgresql"
}

generator client {
  provider            = "prisma-client"
  output              = "../generated"
  moduleFormat        = "cjs"
  importFileExtension = "ts"
}
