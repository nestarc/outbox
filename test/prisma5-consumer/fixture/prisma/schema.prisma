generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ConsumerProbe {
  id String @id @default(uuid()) @db.Uuid

  @@map("consumer_probe")
}
