generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Order {
  id        String @id @default(uuid()) @db.Uuid
  total     Int
  tenantId  String
  requestId String

  @@map("packed_example_orders")
}
